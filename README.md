# BrowserCV_AnnotationTool
A browser tool that lets a user load an image, draw spatial annotations on it (boxes, and optionally other shapes), attach labels and custom attributes to each annotation, and export the result as structured JSON suitable for downstream computer vision / dataset work.
